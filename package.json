{
  "name": "barashop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "bara": "node --experimental-sqlite scripts/barashop-cli.js"
  },
  "devDependencies": {
    "vite": "latest",
    "wrangler": "latest"
  }
}
